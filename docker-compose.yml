version: '3.8'
services:
  api:
    build: fastapi_agent_project
    ports:
      - '8000:8000'
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: fastapi_db
    ports:
      - '5432:5432'
